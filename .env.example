# ================================
# PUBLIC ENVIRONMENT VARIABLES
# ================================
# These are available in the browser (client-side)
# Only use for non-sensitive configuration
PUBLIC_API_BASE_URL=https://dummyjson.com

# ================================
# PRIVATE ENVIRONMENT VARIABLES
# ================================
# These are only available server-side for security
# Use for sensitive configuration that shouldn't be exposed to the browser
API_BASE_URL=https://dummyjson.com
API_TIMEOUT=10000
API_RETRY_ATTEMPTS=3
API_RETRY_DELAY=1000

# ================================
# AUTH.JS CONFIGURATION
# ================================
# Secret key for JWT signing (generate with: openssl rand -base64 32)
AUTH_SECRET=your-super-secret-auth-key-here-replace-in-production
# Login endpoint path for credentials provider
PUBLIC_LOGIN_ENDPOINT=/auth/login

# Example configurations for different environments:

# Development
# PUBLIC_API_BASE_URL=http://localhost:3001
# API_BASE_URL=http://localhost:3001
# API_TIMEOUT=15000

# Staging
# PUBLIC_API_BASE_URL=https://staging-api.example.com
# API_BASE_URL=https://staging-api.example.com
# API_TIMEOUT=12000

# Production
# PUBLIC_API_BASE_URL=https://api.example.com
# API_BASE_URL=https://api.example.com
# API_TIMEOUT=8000